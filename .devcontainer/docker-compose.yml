name: epawm_devcontainer

services:
  app:
    build:
      context: ../
      dockerfile: Dockerfile.dev
    container_name: "${COMPOSE_PROJECT_NAME}"
    # Keep container running
    entrypoint: /bin/bash -c "while sleep 1000; do :; done"
    networks:
      - epawm-net

networks:
  epawm-net: